[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "cleanmarl"
authors = [{name = "Amine Andam", email = "andamamine83@gmail.com"}]
dynamic = ["version", "description"]
